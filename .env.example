# Environment Configuration for Pragmatic Moral Governance Analysis Pipeline
# Copy this file to .env and configure paths for your system

# OpenAI API Key (required only for reliability analysis - krippendorff-all)
OPENAI_API_KEY=your_openai_api_key_here

# Data paths - adjust these to match your system
# Defaults keep everything inside the repository root
PROJECT_ROOT=.
DATA_DIR=${PROJECT_ROOT}/data
DOCS_DIR=${DATA_DIR}/docs
JSON_DIR=${DATA_DIR}/json

# Database path (used by some legacy scripts)
DB_PATH=${DATA_DIR}/pragmatic_moral_governance.db

# Output directories (usually don't need to change)
EMBEDDINGS_OUT=task2_cleanup_and_build_database/out
GRAPHS_OUT=task3_analyse_data/graphs_and_maps
